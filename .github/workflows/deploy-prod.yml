name: BetterFinco prod Frontend deploy

on:
    push:
        branches: [betterfinco-prod]
jobs:
    deploy:
        runs-on: ubuntu-latest
        environment: prod
        strategy:
            matrix:
                node-version: [20.x]
        steps:
            - uses: actions/checkout@master
            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}
            - name: Install Dependencies
              run: npm install
            - name: Set target env
              run: |
                echo "TARGET_ENV=production" >> $GITHUB_ENV
            - name: Build Application
              run: npm run build:prod
            - name: Clean S3 Bucket
              run: |
                  aws s3 rm s3://${{ vars.AWS_BUCKET }} --recursive
              env:
                  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                  AWS_REGION: ${{ vars.AWS_REGION }}
            - uses: jakejarvis/s3-sync-action@master
              name: Upload App to S3 Bucket
              with:
                  args: --follow-symlinks --delete
              env:
                  AWS_S3_BUCKET: ${{ vars.AWS_BUCKET }}
                  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                  AWS_REGION: ${{ vars.AWS_REGION }}
                  SOURCE_DIR: 'dist/web-app'